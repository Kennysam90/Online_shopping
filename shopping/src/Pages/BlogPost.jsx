import PageTemplate from './PageTemplate'

export default function BlogPost() {
  return <PageTemplate title="Blog Post" description="Render individual article content with related reading and sharing actions." />
}
