import PageTemplate from './PageTemplate'

export default function Cart() {
  return <PageTemplate title="Cart" description="Review selected items, quantities, promo codes, and checkout readiness." />
}
